From b43ccc6440846bb298737f6d030a01f20a5a3ba8 Mon Sep 17 00:00:00 2001
From: Hannes Domani <ssbssa@yahoo.de>
Date: Sat, 29 Aug 2015 15:31:46 +0200
Subject: [PATCH 03/11] fix binmode

---
 mingw-w64-crt/crt/binmode.c |  7 ++++---
 mingw-w64-crt/crt/crtexe.c  | 12 ++++++++++++
 2 files changed, 16 insertions(+), 3 deletions(-)

diff --git a/mingw-w64-crt/crt/binmode.c b/mingw-w64-crt/crt/binmode.c
index f0473b08..2e46c2fd 100644
--- a/mingw-w64-crt/crt/binmode.c
+++ b/mingw-w64-crt/crt/binmode.c
@@ -4,7 +4,8 @@
  * No warranty is given; refer to the file DISCLAIMER.PD within this package.
  */
 
-#define SPECIAL_CRTEXE
-
 #include <fcntl.h>
-#include <stdlib.h>
+
+/* Set default file mode to binary */
+
+int _fmode = _O_BINARY;
diff --git a/mingw-w64-crt/crt/crtexe.c b/mingw-w64-crt/crt/crtexe.c
index 361afd23..77f674f7 100644
--- a/mingw-w64-crt/crt/crtexe.c
+++ b/mingw-w64-crt/crt/crtexe.c
@@ -24,6 +24,8 @@
 #include <tchar.h>
 #include <sect_attribs.h>
 #include <locale.h>
+#include <stdio.h>
+#include <fcntl.h>
 
 #if defined(__SEH__) && (!defined(__clang__) || __clang_major__ >= 7)
 #define SEH_INLINE_ASM
@@ -143,6 +145,16 @@ pre_c_init (void)
   else
     __set_app_type (_CONSOLE_APP);
 
+  if (_fmode == _O_BINARY)
+    {
+      if (stdin)
+	_setmode (stdin->_file,_O_BINARY);
+      if (stdout)
+	_setmode (stdout->_file,_O_BINARY);
+      if (stderr)
+	_setmode (stderr->_file,_O_BINARY);
+    }
+
   * __p__fmode() = _fmode;
   * __MINGW_IMP_SYMBOL(_commode) = _commode;
 
-- 
2.15.1.windows.2

