From 842c8f42a55798d60f65fe36af96029ce2280d42 Mon Sep 17 00:00:00 2001
From: Hannes Domani <ssbssa@yahoo.de>
Date: Sat, 29 Aug 2015 16:03:07 +0200
Subject: [PATCH 07/11] free argv[]

---
 mingw-w64-crt/crt/crtexe.c | 10 ++++++++++
 1 file changed, 10 insertions(+)

diff --git a/mingw-w64-crt/crt/crtexe.c b/mingw-w64-crt/crt/crtexe.c
index 602c57c..1f53604 100644
--- a/mingw-w64-crt/crt/crtexe.c
+++ b/mingw-w64-crt/crt/crtexe.c
@@ -77,6 +77,7 @@ DWORD __mingw_winmain_nShowCmd = SW_SHOWDEFAULT;
 static int argc;
 extern void __main(void);
 static _TCHAR **argv;
+static _TCHAR **argv_copy;
 static _TCHAR **envp;
 
 static int argret;
@@ -330,6 +331,8 @@ __tmainCRTStartup (void)
 				    StartupInfo.wShowWindow : SW_SHOWDEFAULT;
       }
     duplicate_ppstrings (argc, &argv);
+    argv_copy = (_TCHAR **) malloc (sizeof (_TCHAR *) * argc);
+    memcpy (argv_copy, argv, sizeof (_TCHAR *) * argc);
     __main ();
 #ifdef WPRFLAG
     __winitenv = envp;
@@ -342,6 +345,13 @@ __tmainCRTStartup (void)
 #endif
     mainret = main (argc, argv, envp);
 #endif
+    {
+      int i;
+      for (i=0; i < argc; i++)
+        free (argv_copy[i]);
+      free (argv_copy);
+      free (argv);
+    }
     if (!managedapp)
       exit (mainret);
 
-- 
1.9.4.msysgit.2

