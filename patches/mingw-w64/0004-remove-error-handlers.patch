From 75e9ea120f808ca027c11cf59e9a73be0cacbb44 Mon Sep 17 00:00:00 2001
From: Hannes Domani <ssbssa@yahoo.de>
Date: Sat, 29 Aug 2015 15:33:16 +0200
Subject: [PATCH 04/10] remove error handlers

---
 mingw-w64-crt/crt/crtexe.c       | 6 ++++++
 mingw-w64-crt/crt/pseudo-reloc.c | 4 ++++
 2 files changed, 10 insertions(+)

diff --git a/mingw-w64-crt/crt/crtexe.c b/mingw-w64-crt/crt/crtexe.c
index 0f42faf04..ef5d8f979 100644
--- a/mingw-w64-crt/crt/crtexe.c
+++ b/mingw-w64-crt/crt/crtexe.c
@@ -104,6 +104,7 @@ static void __cdecl pre_cpp_init (void);
 _CRTALLOC(".CRT$XIAA") _PIFV __mingw_pcinit = pre_c_init;
 _CRTALLOC(".CRT$XCAA") _PVFV __mingw_pcppinit = pre_cpp_init;
 
+#if 0
 extern int _MINGW_INSTALL_DEBUG_MATHERR;
 
 #ifdef __MINGW_SHOW_INVALID_PARAMETER_EXCEPTION
@@ -123,6 +124,7 @@ __mingw_invalidParameterHandler (const wchar_t * __UNUSED_PARAM_1(expression),
   wprintf(L"Expression: %s\n", expression);
 #endif
 }
+#endif
 
 static int __cdecl
 pre_c_init (void)
@@ -151,10 +153,12 @@ pre_c_init (void)
 #else
   _setargv();
 #endif
+#if 0
   if (_MINGW_INSTALL_DEBUG_MATHERR == 1)
     {
       __setusermatherr (_matherr);
     }
+#endif
 
   if (__globallocalestatus == -1)
     {
@@ -283,7 +287,9 @@ __tmainCRTStartup (void)
 #if defined(__x86_64__) && !defined(__SEH__)
     __mingw_init_ehandler ();
 #endif
+#if 0
     _set_invalid_parameter_handler (__mingw_invalidParameterHandler);
+#endif
     
     _fpreset ();
 
diff --git a/mingw-w64-crt/crt/pseudo-reloc.c b/mingw-w64-crt/crt/pseudo-reloc.c
index d4589ca66..6626eda69 100644
--- a/mingw-w64-crt/crt/pseudo-reloc.c
+++ b/mingw-w64-crt/crt/pseudo-reloc.c
@@ -80,6 +80,9 @@ typedef struct {
   DWORD version;
 } runtime_pseudo_reloc_v2;
 
+#if 1
+#define __report_error(a...) abort()
+#else
 static void ATTRIBUTE_NORETURN
 __report_error (const char *msg, ...)
 {
@@ -157,6 +160,7 @@ __report_error (const char *msg, ...)
   abort ();
 #endif
 }
+#endif
 
 /* For mingw-w64 we have additional helpers to get image information
    on runtime.  This allows us to cache for pseudo-relocation pass
-- 
2.39.1.windows.1

