From 58cd28a46a320ad7c46a18e2b693527ea83a0027 Mon Sep 17 00:00:00 2001
From: Hannes Domani <ssbssa@yahoo.de>
Date: Sun, 13 Oct 2024 17:44:20 +0200
Subject: [PATCH 22/39] Disable some interceptors on Windows-gcc builds

---
 .../sanitizer_common_interceptors.inc                | 12 +++++++++---
 .../sanitizer_platform_interceptors.h                | 12 +++++++++---
 2 files changed, 18 insertions(+), 6 deletions(-)

diff --git a/libsanitizer/sanitizer_common/sanitizer_common_interceptors.inc b/libsanitizer/sanitizer_common/sanitizer_common_interceptors.inc
index b10ce7fa44a..e1a44dae2a2 100644
--- a/libsanitizer/sanitizer_common/sanitizer_common_interceptors.inc
+++ b/libsanitizer/sanitizer_common/sanitizer_common_interceptors.inc
@@ -7258,7 +7258,13 @@ INTERCEPTOR(SIZE_T, wcslen, const wchar_t *s) {
   COMMON_INTERCEPTOR_READ_RANGE(ctx, s, sizeof(wchar_t) * (res + 1));
   return res;
 }
+#define INIT_WCSLEN                  \
+  COMMON_INTERCEPT_FUNCTION(wcslen);
+#else
+#define INIT_WCSLEN
+#endif
 
+#if SANITIZER_INTERCEPT_WCSNLEN
 INTERCEPTOR(SIZE_T, wcsnlen, const wchar_t *s, SIZE_T n) {
   void *ctx;
   COMMON_INTERCEPTOR_ENTER(ctx, wcsnlen, s, n);
@@ -7266,11 +7272,10 @@ INTERCEPTOR(SIZE_T, wcsnlen, const wchar_t *s, SIZE_T n) {
   COMMON_INTERCEPTOR_READ_RANGE(ctx, s, sizeof(wchar_t) * Min(res + 1, n));
   return res;
 }
-#define INIT_WCSLEN                  \
-  COMMON_INTERCEPT_FUNCTION(wcslen); \
+#define INIT_WCSNLEN                  \
   COMMON_INTERCEPT_FUNCTION(wcsnlen);
 #else
-#define INIT_WCSLEN
+#define INIT_WCSNLEN
 #endif
 
 #if SANITIZER_INTERCEPT_WCSCAT
@@ -10637,6 +10642,7 @@ static void InitializeCommonInterceptors() {
   INIT_UTMPX;
   INIT_GETLOADAVG;
   INIT_WCSLEN;
+  INIT_WCSNLEN;
   INIT_WCSCAT;
   INIT_WCSDUP;
   INIT_WCSXFRM;
diff --git a/libsanitizer/sanitizer_common/sanitizer_platform_interceptors.h b/libsanitizer/sanitizer_common/sanitizer_platform_interceptors.h
index ed60671014d..3145ac48005 100644
--- a/libsanitizer/sanitizer_common/sanitizer_platform_interceptors.h
+++ b/libsanitizer/sanitizer_common/sanitizer_platform_interceptors.h
@@ -165,8 +165,14 @@ SANITIZER_WEAK_IMPORT void *aligned_alloc(__sanitizer::usize __alignment,
 #define SI_LINUX_NOT_FREEBSD 0
 #endif
 
+#if SANITIZER_WINDOWS && defined(__GNUC__)
+#define SI_NOT_WINGCC 0
+#else
+#define SI_NOT_WINGCC 1
+#endif
+
 #define SANITIZER_INTERCEPT_STRLEN SI_NOT_FUCHSIA
-#define SANITIZER_INTERCEPT_STRNLEN (SI_NOT_MAC && SI_NOT_FUCHSIA)
+#define SANITIZER_INTERCEPT_STRNLEN (SI_NOT_MAC && SI_NOT_FUCHSIA && SI_NOT_WINGCC)
 #define SANITIZER_INTERCEPT_STRCMP (SI_NOT_FUCHSIA && SI_NOT_AIX)
 #define SANITIZER_INTERCEPT_STRSTR SI_NOT_FUCHSIA
 #define SANITIZER_INTERCEPT_STRCASESTR (SI_POSIX && SI_NOT_AIX)
@@ -252,7 +258,7 @@ SANITIZER_WEAK_IMPORT void *aligned_alloc(__sanitizer::usize __alignment,
   (SANITIZER_INTERCEPT_PRINTF && SI_GLIBC)
 
 // AIX libc does not export FREXP and FREXPF.
-#define SANITIZER_INTERCEPT_FREXP (SI_NOT_FUCHSIA && SI_NOT_AIX)
+#define SANITIZER_INTERCEPT_FREXP (SI_NOT_FUCHSIA && SI_NOT_AIX && SI_NOT_WINGCC)
 #define SANITIZER_INTERCEPT_FREXPF (SI_POSIX && SI_NOT_AIX)
 #define SANITIZER_INTERCEPT_FREXPL SI_POSIX
 
@@ -551,7 +557,7 @@ SANITIZER_WEAK_IMPORT void *aligned_alloc(__sanitizer::usize __alignment,
 #define SANITIZER_INTERCEPT_MALLOC_USABLE_SIZE (!SI_MAC && !SI_NETBSD)
 #define SANITIZER_INTERCEPT_MCHECK_MPROBE SI_LINUX_NOT_ANDROID
 #define SANITIZER_INTERCEPT_WCSLEN 1
-#define SANITIZER_INTERCEPT_WCSNLEN 1
+#define SANITIZER_INTERCEPT_WCSNLEN SI_NOT_WINGCC
 #define SANITIZER_INTERCEPT_WCSCAT (SI_POSIX || SI_WINDOWS)
 #define SANITIZER_INTERCEPT_WCSDUP SI_POSIX
 #define SANITIZER_INTERCEPT_SIGNAL_AND_SIGACTION (!SI_WINDOWS && SI_NOT_FUCHSIA)
-- 
2.53.0

