From ca5d72567969f4a6d2d649bb3797121394124a84 Mon Sep 17 00:00:00 2001
From: Hannes Domani <ssbssa@yahoo.de>
Date: Sun, 28 Mar 2021 15:13:14 +0200
Subject: [PATCH 11/15] Warn when a non-trivial class instance is unused (PR
 c++/60212)

---
 gcc/common.opt | 4 ++++
 gcc/cp/decl.c  | 2 ++
 gcc/cp/init.c  | 1 +
 3 files changed, 7 insertions(+)

diff --git a/gcc/common.opt b/gcc/common.opt
index 87456c711a8..311d821def9 100644
--- a/gcc/common.opt
+++ b/gcc/common.opt
@@ -810,6 +810,10 @@ Wunused-label
 Common Var(warn_unused_label) Warning EnabledBy(Wunused)
 Warn when a label is unused.
 
+Wunused-non-trivial
+Common Var(warn_unused_non_trivial) Warning
+Warn when a non-trivial class instance is unused.
+
 Wunused-parameter
 Common Var(warn_unused_parameter) Warning EnabledBy(Wunused && Wextra)
 Warn when a function parameter is unused.
diff --git a/gcc/cp/decl.c b/gcc/cp/decl.c
index b81de8ef934..173c9b31c98 100644
--- a/gcc/cp/decl.c
+++ b/gcc/cp/decl.c
@@ -663,6 +663,7 @@ poplevel (int keep, int reverse, int functionbody)
 		: (DECL_NAME (decl) && !DECL_ARTIFICIAL (decl)))
 	    && type != error_mark_node
 	    && (!CLASS_TYPE_P (type)
+		|| warn_unused_non_trivial
 		|| !TYPE_HAS_NONTRIVIAL_DESTRUCTOR (type)
 		|| lookup_attribute ("warn_unused",
 				     TYPE_ATTRIBUTES (TREE_TYPE (decl)))))
@@ -17825,6 +17826,7 @@ cxx_maybe_build_cleanup (tree decl, tsubst_flags_t complain)
 
   if (cleanup
       && DECL_P (decl)
+      && !warn_unused_non_trivial
       && !lookup_attribute ("warn_unused", TYPE_ATTRIBUTES (TREE_TYPE (decl)))
       /* Treat objects with destructors as used; the destructor may do
 	 something substantive.  */
diff --git a/gcc/cp/init.c b/gcc/cp/init.c
index a85f4d50750..53df3c6098e 100644
--- a/gcc/cp/init.c
+++ b/gcc/cp/init.c
@@ -1847,6 +1847,7 @@ build_aggr_init (tree exp, tree init, int flags, tsubst_flags_t complain)
 
   if ((VAR_P (exp) || TREE_CODE (exp) == PARM_DECL)
       && TREE_SIDE_EFFECTS (stmt_expr)
+      && !warn_unused_non_trivial
       && !lookup_attribute ("warn_unused", TYPE_ATTRIBUTES (type)))
     /* Just know that we've seen something for this node.  */
     TREE_USED (exp) = 1;
-- 
2.15.1.windows.2

